{
  "name": "@amxx/privacess",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "wsrun": "^5.2.4"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
